<div class="body-height">
<div class="container">
	<h1>New Post</h1>

	<%= form_for @music, :url => {controller:  :musics, action: :create} do |f| %>
	<%= f.attachment_field :post_image %>
      <h3>Artist<%= f.text_field :artist %></h3>
      <h3>title<%= f.text_field :title %></h3>
      <h3>point<%= f.text_area :impression %></h3>
      <h3>genre<%= f.collection_select :genre_id, Genre.all,:id,:category %></h3>
      <h3>location<%#= f.select :genre_id, ['夏に聞きたい曲','冬に聞きたい曲','クリスマスソング',
        'ドライブ','通勤','家事','ティータイム','リフレッシュ,リラックス'], {include_blank: '---'} %></h3>
        <%#= f.collection_select :genre, Genre.all, :id, :category %>
      <%= f.submit '新規登録', class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
<%#= f.select :genre_id, ['R&B', 'J-POP', 'HOUSE Music', 'アニソン','ヒーリングミュージック','JAZZ','K-POP','HIP HOP'], {include_blank: '---'} %></h3>